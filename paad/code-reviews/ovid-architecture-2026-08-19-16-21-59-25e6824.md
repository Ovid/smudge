# Agentic Code Review: ovid/architecture

**Date:** 2026-08-19 16:21:59
**Branch:** `ovid/architecture` -> `main`
**Commit:** `25e68244b9529c3b31a055c3856111f3a0ceb8b2`
**Files changed:** 17 | **Lines changed:** +1160 / -125
**Diff size category:** Large

## Executive Summary

This is the **third** review round on this architecture fix session (F-07, F-16, F-18), and it is
the second consecutive round to find that the F-07 forcing-pause test does not block the shape it
is recorded as blocking. Two **Important** in-scope findings, both in
`packages/client/src/__tests__/mutationCommittedSurface.test.ts`: the round-2 `[I1]` and `[I2]`
fixes each closed their own hole but disagree about which handle set to scan, so their
intersection is still open (V1); and the one-step `const { run } = useEditorMutation(…)`
destructure — the exact shape round-2 `[I1]` named — still passes green inside any file already
listed in `COMMITTED_CALLERS`, `pages/EditorPage.tsx` included (V2). Confidence is high: the
verifier reproduced both by executing the shipped helper bodies verbatim under `tsx` against real
repository source rather than reasoning about the regexes.

Nothing critical. **F-16 (the store slice move) verified completely clean** by four independent
specialists — pure move, character-identical signatures, `METHODS_BY_DOMAIN` union unchanged, only
one slice-typed parameter in the tree and it is unaffected. **F-18 (the banner consolidation)
verified behaviourally faithful** — role, `aria-live`, every class token and both dismiss labels
preserved, no orphaned `ActionErrorBanner` reference anywhere, no surviving duplicate of the
consolidated shape. Its one residual (V3) is a type-level gap in the S5 prop union with no live
call site. The remaining eight suggestions are split between decoupled test assertions (V4, V5, V6)
and documentation whose claims outrun what the code enforces (V7–V12) — which in this repo is a
real defect class, since `CLAUDE.md` and the architecture report's `Status:` blocks are the
mechanism by which a future session skips verification it would otherwise redo.

## Critical Issues

None found.

## Important Issues

### [I1] `findUncountableRunShapes` scans per-file handles while `countMutationRuns` was widened to tree-wide handles — the round-2 `[I1]`/`[I2]` fixes leave their intersection open
- **File:** `packages/client/src/__tests__/mutationCommittedSurface.test.ts:142` (the `for (const name of extractMutationHandles(source))` loop), consumed at `:245`; contrast `:210` (`allHandles`) → `:224` (`countMutationRuns(source, allHandles)`)
- **Bug:** Commit `ee92d5e3` widened `discoverCallers()` to count run-calls against handle names bound **anywhere in the tree**. Commit `6b77d25b` added `findUncountableRunShapes` as the backstop for the one call shape the counter cannot attribute. The backstop was never widened — it still iterates only the scanned file's **own** bindings. A caller combining both shapes escapes both fixes. Verified by executing the shipped helpers against:

  ```ts
  import type { XDeps } from "./useX.types";
  export function useX(deps: XDeps) { const { run } = deps.mutation; return run(async () => {}); }
  ```

  `extractMutationHandles` → `[]`; `countMutationRuns(src, ["mutation"])` → `0`;
  `importPatternFor("useEditorMutation").test(src)` → `false`; `findUncountableRunShapes(src)` → `[]`.
  All three signals at `:225` are false, so the file is `continue`d — no key, no offender. Passing
  the tree-wide handle set into the offender pass instead yields
  ``["destructures run() off the `mutation` handle"]``, confirming the fix works.
- **Impact:** `useEditorMutation.ts`'s `finally` dispatches no terminal event on the `reloadFailed` path, so an unguarded caller leaves the machine at `editable:false` with no lock banner — the writer stranded read-only with nothing on screen explaining it (CLAUDE.md §Editor operational state). That is the exact F-07 defect this test is the whole deliverable for. The precondition is not contrived: `FindReplaceControllerDeps` (`useFindReplaceController.ts:25`) and `SnapshotControllerDeps` (`useSnapshotController.ts:63`) are already exported interfaces one hoist away, and `hooks/useProjectEditor.types.ts` is the established local precedent the file's own header cites at `:80-82`.
- **Honest scoping (verified separately):** the sibling `*.types.ts` that hoists the deps interface *is* discovered — it yields `handles: ["mutation"]`, `runs: 0`, `importsHook: true`, so test 1 does go red with a new key. But the red fires on the **types file**, and the resolution its own error message invites (adding `"hooks/useNewController.types.ts": 0` to `COMMITTED_CALLERS`) restores green without anyone looking at the actual caller. Not a fully silent green; a forcing pause pointing at the wrong file, whose obvious fix silences it.
- **Suggested fix:** give `findUncountableRunShapes(source, handles = extractMutationHandles(source))` the same defaulted second parameter `countMutationRuns` already has at `:163-166`, and pass `allHandles` from the `:238` test. Over-flagging fails toward a false RED, which the file declares the safe direction at `:208-209`. Add the two-file fixture as a self-test — every existing case in that describe block binds its handle locally, which is why the gap survived.
- **Confidence:** Medium (78)
- **Found by:** Logic & Correctness (B), Error Handling & Edge Cases (B), Contract & Integration (B) — three independent reproductions (`claude-opus-5[1m]`)

### [I2] The one-step `const { run } = useEditorMutation(…)` destructure still passes green inside a file already on the committed surface — including `pages/EditorPage.tsx`
- **File:** `packages/client/src/__tests__/mutationCommittedSurface.test.ts:139-147` and `:225`; the claims it undercuts are at `paad/architecture-reviews/2026-08-11-smudge-architecture-report.md:305` and `:311`
- **Bug:** A **second, independent hole**, not a restatement of I1 — the verifier confirmed I1's fix does not close it. Round-2 finding `[I1]` named the one-step destructure as one of the shapes that binds no handle, and the fix added the *import* signal to `discoverCallers()`. The import signal only converts a file from **invisible to listed**. For a file already in `COMMITTED_CALLERS` it changes nothing. Verified by executing the shipped helpers over the real `packages/client/src/pages/EditorPage.tsx` with one such caller appended:

  | probe | before | after |
  |---|---|---|
  | `extractMutationHandles` | `["mutation"]` | `["mutation"]` |
  | `countMutationRuns(src, ["mutation"])` | 0 | 0 |
  | `findUncountableRunShapes` | `[]` | `[]` |
  | `countCodeMatches(src, COMMITTED_RE)` | 0 | 0 |

  Both assertions compare unchanged numbers → **green**. Mechanism: `HANDLE_RE`'s `(\w+)` cannot
  bind `{`, so no handle is added; the destructure regex at `:143` requires `\bmutation\b`
  (lowercase) after the `=`, which `useEditorMutation(` does not supply — the capital `M` breaks
  the word match. Applying I1's tree-wide-handles fix to this fixture still yields `[]`, which is
  how the two findings were confirmed distinct.
- **Impact:** `EditorPage.tsx` is the file that *owns* the shared `useEditorMutation` instance and is where CLAUDE.md's F-1 note says editor-mutating entry points concentrate — the single most likely host for a third caller. The report says of that exact file at `:307` "the day it starts calling `run()` the assertion goes red", which holds only for the `mutation.run(` spelling; `:311` asserts the destructure shape is closed ("`findUncountableRunShapes` refuses the one unattributable call shape outright"); and the greppable one-liner at `:305` states the detection clause without qualification, which the test file's own header at `:65-68` explicitly retracts ("do not assume the numbers will notice"). Two review rounds have now found this detector green-passing shapes it was recorded as blocking.
- **Suggested fix:** make the refusal handle-independent — additionally flag `\{[^{}]*\brun\b[^{}]*\}\s*=\s*[^;\n]*useEditorMutation\s*[<(]`, so the shape is refused whether or not the file is already listed. Then narrow `:305` and `:311`: fold the ceiling into the Status line rather than leaving it only in the caveat three lines down.
- **Confidence:** High (84)
- **Found by:** Spec Compliance (A + B, merged) (`claude-opus-5[1m]`)

## Suggestions

- **[S1] `children: ReactNode` makes the S5 union enforce prop *presence*, not control presence** — `packages/client/src/components/EditorBanner.tsx:51-55`. `ReactNode` admits `null`/`undefined`/`false`, so `<EditorBanner tone="error" message="x">{cond && <button/>}</EditorBanner>` type-checks (verified with a real `tsc --noEmit` probe) and renders an assertive `role="alert"` with no control when `cond` is false. Three texts claim the stronger guarantee — the comment at `:41`, the test comment at `EditorBanner.test.tsx:53-55`, and report `:482`. No current call site triggers it; the plausible future edit is a conditional Refresh at `EditorMainContent.tsx:233-239`. Fix: narrow the arm to `children: ReactElement` (one token) and add a companion `@ts-expect-error` case passing `{false}`. *(Logic & Correctness A + Error Handling A + Contract & Integration A, merged; Medium 65)*
- **[S2] S4's "single owner of the `.run(` shape" left four hand-built copies in the same file, plus a stale comment** — `packages/client/src/__tests__/migrationStructuralCheck.test.ts:179-186` (comment), `:276`, `:280-282`, `:349`, `:361`. The production call at `:187` now uses `runCallPattern`, but four local regexes survive writing the pre-S4 shape, and the comment above the changed line still documents the removed regex ("Inner `[^>]*` is non-nested by design") while the same commit added a self-test at `:381` asserting nested generics now match. Round-2 S4 named `:356`/now-`:361` by line, so **the fix is incomplete relative to the finding it closes**. If `runCallPattern` were loosened back toward receiver-blindness, `:349` stays green and the whole-tree assertion at `:204` notices only by producing *fewer* offenders — a silent pass. No divergence today. Fix: call `runCallPattern(bindings[0])` at the three sites, lift the delegation matcher into an exported helper, rewrite `:179-186` and `:79-81`. *(Contract & Integration B + Logic & Correctness B + Spec Compliance A, merged; High 80)*
- **[S3] The pinned "remaining ceiling" is false in its load-bearing half — the stripper can *delete* real code, not merely under-strip** — `packages/client/src/__tests__/tsSourceScan.ts:30-33` and `migrationStructuralCheck.test.ts:409-416`. Both texts assert the consequence is a comment left unstripped, "a false RED … not a silent green" — the entire justification for accepting the ceiling over a tokenizer. Verified: a file with `const re = /"/;` above `glob("**/*.ts")` strips to `const re = /"/;\nconst files = glob("**\nexport {};`, erasing the handle binding, the `run()` call and the committed branch; counts go **down**. The pinned assertion at `:415-416` is `toContain("//")` — it tests only that *a* comment survived, not the direction claim three lines above. Latent: no such literal exists in `packages/client/src` today (differentially confirmed against the TypeScript parser over every non-test file). Fix: correct both texts and extend the self-test with the erasure fixture, or take the documented tokenizer upgrade path. *(Logic & Correctness B; Medium 71)*
- **[S4] `COMMITTED_CALLERS` is asserted equal to two independently-derived quantities, and the header's stated rationale contradicts the implementation** — `packages/client/src/__tests__/mutationCommittedSurface.test.ts:193`, asserted at `:235` (run-call count) and `:256` (branch count). When the two legitimately diverge — two `run()` calls sharing one handler, which the header at `:52-55` explicitly anticipates; or one `run()` needing two comparisons for the two-sub-case shape at `:37-41` — no integer satisfies both, so the demanded decision **cannot be encoded** and the only route back to green is weakening the detector. This is not the acknowledged "does not verify handling is CORRECT" caveat at `:43-50`, which is about false greens. Fix: make the map value `{ runs, committedBranches }`, and correct the "because a file's callers may share one handler" sentence. *(Error Handling & Edge Cases B; Medium 62)*
- **[S5] CLAUDE.md's summary of the fix-session carve-out enumerates four of its six rules — and the two omitted are the ones a session needs** — `CLAUDE.md:573` vs `docs/roadmap-decisions/2026-08-19-architecture-fix-session-pr-scope.md:43-60`. Commit `da27f929` wrote the exhaustive-sounding four-item summary; `1b826b4f` added rule 5 (untagged Safety Net commit allowed) and rule 6 (follow-ups need no `Status:` block) and did not touch it. The decision doc's own Amendments section records that a session reading the four-item list literally "would have found its own mandatory Safety Net commit unauthorised" — which applies with more force to CLAUDE.md, the file loaded automatically at session start. Fix: append the two rule classes, or drop the enumeration and let it be a pure pointer. *(Spec Compliance A + B, merged; Medium 77)*
- **[S6] Rule 6 designates `git log --oneline main..HEAD` as the traceability surface, but every follow-up tag on this branch is ambiguous** — `docs/roadmap-decisions/2026-08-19-architecture-fix-session-pr-scope.md:48-56`. Two reports raised findings on this branch and the tags carry no report qualifier, producing six collided pairs across all twelve follow-up commits: `[I1]` = `46ff26fb`/`ee92d5e3`; `[I2]` = `36169a38`/`6b77d25b`; `[S1]` = `61d1c12f` (lock banner)/`61ba2cc2` (F-16 note); `[S2]` = `6eae2ee8`/`1b826b4f`; `[S3]` = `6eae2ee8`/`25e68244`; `[S4]` = `da27f929` (carve-out doc)/`02ba9344` (regex helper). Rule 6 *declines* to require a `Status:` block on the grounds that the log suffices — a trade only sound if the log is unambiguous, which it demonstrably is not on the branch the rule was written for. Fix: require a report-qualifying tag (`[r2 S2]` or `[da27f92 S2]`). *(Spec Compliance A + B, merged; Medium 78)*
- **[S7] Commit `6eae2ee8` violates rule 6, and rule 6 was written *because of* that commit** — `docs/roadmap-decisions/2026-08-19-architecture-fix-session-pr-scope.md:51`. Rule 6 imports rule 2's "one finding per commit" shape; `6eae2ee8 docs(report): [S2][S3] …` carries two. Round-2's S2 asked whether "one finding per commit" binds the follow-ups; rule 6 answers yes, which **converts the pre-existing shape into an explicit violation** rather than resolving it. Distinct from S6 (batching vs ambiguity, different fix). This is the only precedent a future `/paad:fix-architecture` session has. Fix: split `6eae2ee8` (pure docs, cheap rebase), or amend rule 6 to allow same-artifact batching and record `6eae2ee8` as the precedent. *(Spec Compliance B; High 80)*
- **[S8] Rule 5 places the Safety Net commit "at the head of the branch", contradicting its own preceding sentence and the branch's actual layout** — `docs/roadmap-decisions/2026-08-19-architecture-fix-session-pr-scope.md:43-47`. The rule says behaviour must be pinned "*before* any fix commit lands", then says the commit sits "at the head of the branch" — the tip, i.e. newest. On this branch `89bc9361` is the **oldest** commit (`git log main..HEAD --reverse` lists it first). Aggravated eleven lines later where the same doc uses `main..HEAD` with HEAD meaning the tip. Fix: "at the base of the branch, before the first fix commit". *(Spec Compliance B; Medium 68)*
- **[S9] Both new `Status date:` fields are local time labelled UTC, and post-date the commit that wrote them** — `paad/architecture-reviews/2026-08-11-smudge-architecture-report.md:313` and `:483`, both `2026-08-19 14:52 UTC`. Verified against `git show -s --format=%aI`: the branch authors at `+0200`, so F-07's last listed commit `3b3d67a5` is 12:49 UTC, F-18's `71a8a5e5` is 12:47 UTC, and the commit that *wrote* both fields (`25e68244`) is 13:47 UTC — the recorded stamp is an hour in the future relative to its own authorship. The neighbouring F-16 entry at `:455` follows the real convention correctly. `Status date` is one of two fields a future session greps to order a finding's history against the log, and round-2's S3 was raised precisely because that pairing had drifted. Fix: use 12:49 / 12:47 UTC, or drop the `UTC` label and record the offset. *(Spec Compliance A; Medium 68)*
- **[S10] The `*** STOP` block hands a third caller's author the instruction F-07's Status says is no longer the right one** — `paad/architecture-reviews/2026-08-11-smudge-architecture-report.md:308` vs `packages/client/src/__tests__/mutationCommittedSurface.test.ts:36-41`. The Status block records a deferred decision: moving the terminal dispatch into `run()` "remains available and is the right one if a third consumer ever lands", and the trigger for that decision is exactly the moment this test goes red on a new caller. The `*** STOP` block the author reads at that moment tells them to add a handler and points at CLAUDE.md §Editor operational state — not at the recorded conclusion that a third caller is the signal to fix the **seam** instead. Nothing else ties the decision to its trigger (backlog unchanged at 24 entries, no roadmap item). Fix: one line in the `*** STOP` block. *(Spec Compliance B; Medium 62)*

## Out of Scope

> **Handoff instructions for any agent processing this report:** The findings below are
> pre-existing bugs that this branch did not cause or worsen. Do **not** assume they
> should be fixed on this branch, and do **not** assume they should be skipped.
> Instead, present them to the user **batched by tier**: one ask for all out-of-scope
> Critical findings, one ask for all Important, one for Suggestions. For each tier, the
> user decides which (if any) to address. When you fix an out-of-scope finding, remove
> its entry from `paad/code-reviews/backlog.md` by ID.

### Out-of-Scope Critical
None found.

### Out-of-Scope Important
None found.

### Out-of-Scope Suggestions

#### [OOSS1] `CLAUDE.md:410` says "7-slice interface"; the tree has eight, and this branch's own F-16 Status note says "eight slices" — backlog id: `8d666157`
- **File:** `CLAUDE.md:410` vs `packages/server/src/stores/project-store.types.ts:156-165` and `paad/architecture-reviews/2026-08-11-smudge-architecture-report.md:451`
- **Bug:** Verified at HEAD — `ProjectStore` extends eight slices (`ProjectsStore`, `ChaptersStore`, `ChapterStatusesStore`, `SettingsStore`, `VelocityStore`, `ImagesStore`, `SnapshotsStore`, `OuttakesStore`), while CLAUDE.md's F-4 accepted-trade-off entry reads "The 7-slice interface's documented data-surface value justifies the type surface". The F-16 Status note **added by this branch** says "extends all eight slices", so two documents in the repo now give different counts for the same interface.
- **Impact:** The count is load-bearing to F-4's argument — F-4 was accepted *because* the slice decomposition has documented value, and F-16 is a finding about a method living in the wrong slice. A session reconciling "which slices exist" gets a mismatch at exactly the boundary both entries are about.
- **Scope reasoning:** `CLAUDE.md:410` is not in the touched-lines map — the branch's only CLAUDE.md edit is the new line 573. `OuttakesStore` predates this branch, and the F-16 move relocated methods *between* existing slices without changing the count, so no reasoning promotion applies. The branch **surfaced** the drift rather than causing it.
- **Suggested fix:** Change to "8-slice", or to "per-domain slice" so the sentence stops needing maintenance when a slice is added. Note open architecture finding **F-19** ("The steering file has drifted from the tree") covers this class but does not list this instance — triage may prefer folding it there rather than carrying it in the backlog.
- **Confidence:** Medium (72)
- **Found by:** Spec Compliance (`claude-opus-5[1m]`)
- **Backlog status:** new

## Out-of-Scope Additions

> **Handoff instructions for any agent processing this report:** The entries below are code this branch added that the spec did not promise. They may be legitimate "while I'm here" fixes for issues exposed by this work, or scope creep that should live in a separate PR. Do **not** assume they should stay on this branch, and do **not** assume they should be reverted. Present them to the user **as a single batched ask**: "These M additions weren't promised by the spec — keep, split into a separate PR, or revert?" The user decides per item.
>
> Out-of-scope additions are flagged for this PR only — they do not persist to `paad/code-reviews/backlog.md`.

### [OOSA1] `tsSourceScan.ts` and the behaviour changes it pushed into the pre-existing `migrationStructuralCheck.test.ts`
- **File:** `packages/client/src/__tests__/tsSourceScan.ts:1-100` (new module); `packages/client/src/__tests__/migrationStructuralCheck.test.ts:1-13, 187`
- **Addition:** None of F-07 / F-16 / F-18 asked for a shared scanning module or for edits to the abortable-op drift detector. The module was created by `36169a38` and has grown twice since: `02ba9344` moved the `.run(` matcher into it and pointed `migrationStructuralCheck`'s production check at it, and `3b3d67a5` made the comment stripper string-literal-aware. Two behaviour changes land on the pre-existing detector as a side effect: (1) its `.run(` matcher is now **looser** — `runCallPattern` additionally tolerates optional chaining, a Prettier wrap, one `.current` hop and nested generics, so fewer bindings are reported as dead; (2) `importPatternFor` now also matches `import type { … }`, so a future type-only import of `useAbortableAsyncOperation` would flip that file into the "imports the hook but has no binding" offender branch (no such import exists today).
- **Suggested intent source:** `paad/architecture-reviews/2026-08-11-smudge-architecture-report.md` findings F-07 / F-16 / F-18 and their `Status:` blocks; `docs/roadmap-decisions/2026-08-19-architecture-fix-session-pr-scope.md` (carve-out rule 6, which authorises review-round follow-up commits).
- **Note — this is a re-surface, not a new item.** It was already presented as `[OOSA1]` in `paad/code-reviews/ovid-architecture-2026-08-19-14-12-00-da27f92.md` and no disposition was recorded. It is re-surfaced rather than dropped because it is the same open PR **and** because the module has grown twice since it was last presented — the two behaviour changes above landed after that report and were not part of what was previously asked. If OOSA1 was already accepted, the only new question is whether those two behaviour changes to the pre-existing detector are accepted too.
- **Confidence:** Medium (70)
- **Found by:** Spec Compliance (`claude-opus-5[1m]`)

## Review Metadata

- **Agents dispatched:** 12 specialists (6 lenses × 2 partitions, per the large-diff rule) + 1 Verifier.
  - Logic & Correctness A (banner UI) / B (scanners + store)
  - Error Handling & Edge Cases A / B
  - Contract & Integration A / B
  - Concurrency & State A / B — **both bailed cleanly** (`BAIL: concurrency-state no-surface`)
  - Security A / B — **both bailed cleanly** (`BAIL: security no-boundary`)
  - Spec Compliance A (code vs intent) / B (documentation & process)
  - **Partitioning:** A = the editor-banner UI surface (`EditorBanner.tsx`, `EditorBanner.test.tsx`, `EditorMainContent.tsx`, deleted `ActionErrorBanner.tsx`, `EditorPageFeatures.test.tsx`, `e2e/chapter-create-recovery.spec.ts`). B = the source-scanner drift detectors and the server store slice move (`tsSourceScan.ts`, `mutationCommittedSurface.test.ts`, `migrationStructuralCheck.test.ts`, `project-store.types.ts`, `sqlite-project-store.ts`, `sqlite-project-store.test.ts`). Spec Compliance split code (A) vs documentation/process (B).
- **Scope:** 17 changed files, plus adjacent callers/callees traced one level deep — client: `pages/EditorPage.tsx`, `strings.ts`, `errors/` (`apiErrorMapper.ts`, `scopes.ts`, `applyMappedError.ts`), `hooks/useEditorMutation.ts`, `hooks/useEditorMutationMachine.ts`, `hooks/useFindReplaceController.ts`, `hooks/useSnapshotController.ts`, `hooks/useAbortableSequence.ts`, `hooks/useAbortableAsyncOperation.ts`, `hooks/useProjectEditor.ts`, `components/Editor.tsx`, `components/EditorFooter.tsx`, `components/SnapshotBanner.tsx`, `components/ImageGallery.tsx`, `__tests__/editorEntryPointSurface.test.ts`, `eslint.config.js`; server: `stores/index.ts`, `images/images.service.ts`, `images/images.references.ts`, `projects/projects.service.ts`, `search/search.service.ts`, `chapters/chapters.repository.ts`, `snapshots/auto-snapshot.ts`, `export/image-resolver.ts`.
- **Raw findings:** 23 (before verification)
- **Verified findings:** 14 (13 bugs + 1 out-of-scope addition)
- **Filtered out:** 9 — **all merged duplicates, zero dropped as false positives.** Every finding above the 60-confidence floor was confirmed by reading or executing the code. Merges: 3→1 for I1, 2→1 for I2, 3→1 for S1, 3→1 for S2, 2→1 for S5, 2→1 for S6.
- **Out-of-scope findings:** 1 (Critical: 0, Important: 0, Suggestion: 1)
- **Out-of-scope additions:** 1
- **Backlog:** 1 new entry added, 0 re-confirmed (see `paad/code-reviews/backlog.md` — 25 total active)
- **Steering files consulted:** `CLAUDE.md` (§Save-pipeline invariants, §Editor operational state, §Accepted Architectural Trade-offs, §API Design, §Testing Philosophy, §String externalization, §Accessibility, §Pull Request Scope). `.devcontainer/` excluded per project rule.
- **Intent sources consulted:** `paad/architecture-reviews/2026-08-11-smudge-architecture-report.md` (findings F-07, F-16, F-18 and their `Status:` blocks); `docs/roadmap-decisions/2026-08-19-architecture-fix-session-pr-scope.md`; the two prior code-review reports on this branch (`ovid-architecture-2026-08-19-12-33-44-6e1ee51.md`, `ovid-architecture-2026-08-19-14-12-00-da27f92.md`); the 21 branch commit messages; `CLAUDE.md` §Pull Request Scope.
- **Verifier warnings:** none

### Verified clean (recorded so a future round does not re-derive it)

- **F-16 (store slice move) — clean on every axis checked.** `git diff` over `project-store.types.ts` / `sqlite-project-store.ts` is a pure move with character-identical signatures, both still delegating to `chaptersRepo`. `METHODS_BY_DOMAIN`'s **sorted union is unchanged**, so the safety-net test's strength is identical before and after. `npm run typecheck` clean across all three packages. Four independent greps confirm the report's claim that `auto-snapshot.ts:37` (`txStore: SnapshotsStore`) is the **only** slice-typed parameter in the tree, and neither moved method belongs to `SnapshotsStore`. No production code names `ImagesStore` or `ChaptersStore` directly; `SqliteProjectStore` is the sole implementer (no fake, no narrow-slice double). `chapters.repository.ts:232-252` uses Knex bindings with no string concatenation; soft-deleted-row reachability is identical.
- **F-18 (banner consolidation) — behaviourally faithful.** Class-set comparison of all three old banner blobs against `TONES`: role, `aria-live`, dismiss `aria-label`, container classes, dismiss-button classes and the Refresh button's classes are set-identical; DOM child order and banner slot order (lock → error → info) preserved. The three slots are positional JSX children, so React's index-keyed `mapRemainingChildren` cannot alias the lock banner's fiber onto the action-error banner now that both are `EditorBanner` — checked specifically, does not occur. Zero live `ActionErrorBanner` references repo-wide; no barrel file existed. `EditorMainContentProps` unchanged, so `editorEntryPointSurface.test.ts` correctly needed no update. No surviving duplicate of the consolidated shape anywhere in the client (`EditorFooter.tsx:21` and `SnapshotBanner.tsx:61` are genuinely different shapes). The `✕` glyph needs no ESLint disable — the selector is `JSXText[value=/\p{L}/u]` (`eslint.config.js:182`), letters-only exactly as CLAUDE.md claims.
- **F-07's "seam deliberately not changed" — TRUE.** `git diff main...HEAD -- packages/client/src/hooks/` is **empty**.
- **The new `EditorPageFeatures` test does not pass by accident.** The two regions are distinguished by their *controls*, not just their roles: if the committed message regressed from `applyReloadFailedLock` to `setActionError` it would still land in a `role="alert"`, but `within(lockRegion).getByRole("button", { name: refreshButton })` would fail. `openPanelAndSearch` restores real timers in a `finally` before the assertions run, so the `waitFor(3000)` is a real wall-clock budget.
- **Security and concurrency: no surface.** No `dangerouslySetInnerHTML` in the banner path; `tone` is a compile-time literal at all three call sites; `err.message` still cannot reach the UI (the `Object.hasOwn` + `typeof === "string"` guard at `apiErrorMapper.ts:129-133` is untouched). The scanner regexes are only ever fed `\w`-constrained names or module-literal constants, so no metacharacter injection is reachable; their worst case is polynomial backtracking over the repo's own committed TypeScript. `HANDLE_RE` is module-scope with `g` but only consumed via `String.prototype.matchAll`, which clones rather than mutates — no stale-`lastIndex` bug. No secrets or credentials introduced.
- **Suite state at HEAD:** 165 files / 3420 tests green. `mutationCommittedSurface.test.ts` (31), `migrationStructuralCheck.test.ts` (10), `EditorBanner.test.tsx` (4), server `sqlite-project-store.test.ts` (48) all pass. No `.only`, no `.skip` in any in-scope file. The `@ts-expect-error` at `EditorBanner.test.tsx:56` is genuinely consumed (TS2578 is unconditional, and `tsc --noEmit -p packages/client` exits 0).
