# Agentic Code Review: scratchpad-outtakes

**Date:** 2026-08-05 08:32:39
**Branch:** scratchpad-outtakes -> main
**Commit:** fce240f680224efb7fc80c2d973d7e3513d193e7
**Files changed:** 154 | **Lines changed:** +13,047 / -949
**Diff size category:** Large

## Executive Summary

Phase 4c.2 (scratchpad outtakes) is structurally sound after eight review rounds: both Security specialists walked their trust boundaries fully and the client side returned zero findings, Concurrency & State on the server returned zero after an explicit (non-bail) audit of all four new transaction boundaries, and Contract & Integration confirmed the migration→repository→store→service→route→wire slice at every seam with a clean typecheck, lint, and full client suite. No Critical issues. The six Important findings cluster into two shapes that have now recurred across rounds: **a fix applied to some siblings but not all** (the OOSS1 image-id case fix reached four of five export formats but not DOCX; the `db36f8a2` 404 reconciliation reached update and delete but not create; the `b082975d` drift guard reached the panel's create but not the card's two writers), and **a walker guard hardened on one line but not the line above it** (`extractText` still gates on truthiness where its sibling gates on `typeof === "string"`). One finding is a genuine regression introduced by this branch's own S8 dedup: `refreshTrashList`'s drift baseline silently moved from the operation's project to the current one, which lets project A's deleted chapters paint into project B's trash view. Confidence is high — the Verifier re-executed the load-bearing claims rather than trusting the specialists, and corrected two sub-claims in the process.

## Critical Issues

None found.

## Important Issues

### [I1] `docx.renderer.ts` never lowercases the image id, so DOCX is now the only export missing an uppercase-referenced image
- **File:** `packages/server/src/export/docx.renderer.ts:390` (extraction at `:376`)
- **Bug:** `const resolved = await resolveImage(idMatch[1], state.imageSource);` — no `.toLowerCase()`. `ALLOWED_IMAGE_SRC` carries the `"i"` flag, so `idMatch[1]` can come back uppercase; it lands on `findImageById` → `db("images").where("id", id)`, a TEXT column under SQLite's default BINARY collation. Commit `5294cdf6` fixed exactly this on the HTML route by canonicalizing before resolving (`image-resolver.ts:137-139`); the DOCX walker is the documented exception that never goes through `resolveImageSrcs`, so it did not receive the fix. The commit message scopes itself to "HTML/Markdown/plaintext/EPUB".
- **Impact:** A chapter node `{"type":"image","attrs":{"src":"/api/images/A1B2C3D4-…"}}` — reachable from a restored backup or hand-edited DB, the same premise the branch accepted when it fixed OOSS1, and pinned as an *accepted* src by `image-src-allowlist-parity.test.ts:101` (`["an uppercase uuid", …, true]`). `extractImageIds` lowercases, so refcounting keeps the image alive and undeletable. HTML/Markdown/plaintext/EPUB embed it. DOCX logs `"Could not resolve image for docx export"` and returns `[]` — the image **and its caption/license attribution** are absent from the .docx handed to a beta reader or publisher. Before `5294cdf6` an uppercase reference failed *uniformly* across all five formats, so DOCX's defect had no distinguishable effect; now the writer's natural cross-check (preview the HTML export) no longer catches it. That divergence is created by this branch's diff. No DOCX-path uppercase test exists — `toUpperCase` in `packages/server/src/__tests__/` appears only at `export.images.test.ts:251,267`, both on the `resolveImagesInHtml` route.
- **Suggested fix:** Canonicalize inside `resolveImage` itself (`image-resolver.ts:60`) so every renderer — including one that does not exist yet — inherits it; the existing `resolveImageSrcs` lowercase then becomes belt-and-braces. Minimum: `resolveImage(idMatch[1].toLowerCase(), …)` plus the two `logger.warn` calls below it.
- **Confidence:** High
- **Found by:** Security (`claude-opus-5[1m]`), Logic & Correctness (`claude-opus-5[1m]`), Contract & Integration (`claude-opus-5[1m]`)

### [I2] `extractText` gates on truthiness where its sibling gates on `typeof === "string"`; a non-string `text` throws out of `countWords`
- **File:** `packages/shared/src/wordcount.ts:30` (throw surfaces at `:62`)
- **Bug:** `if (node.text) return node.text;` returns whatever `text` holds, and `countWords` then calls `.trim()` on it. The sibling walker hardened in this same branch, one file over, type-checks: `tiptap-plaintext.ts:32` reads `if (typeof node.text === "string")`. The line was left untouched while the guards directly below it (`Array.isArray(node.content)` container, `isTipTapNode(child)` child) were added.
- **Impact:** Reproduced end-to-end by the Verifier. `CreateOuttakeSchema.safeParse({content:{type:"doc",text:42}})` → `success: true`; `stripImageNodes` preserves `text: 42`; `outtakes.repository.parseRow`'s `isTipTapNode` gate passes, so `content_corrupt` is **not** set and the row is served as healthy; `OuttakeCard.tsx:304` then calls `countWords(outtake.content)` unconditionally during render — outside the `content_corrupt` ternary — and throws. There is **no** `ErrorBoundary` / `componentDidCatch` / `getDerivedStateFromError` anywhere in `packages/client/src`, so React 18 unmounts the tree. One `POST /api/projects/:id/outtakes` permanently bricks the outtakes drawer, and the row cannot be deleted because the delete button lives on the card that will not render — precisely the "unrenderable and therefore undeletable" failure the degraded-read design exists to prevent, on a hard-delete table with no trash. The pre-existing second consequence (`PATCH /api/chapters/{id}` → uncaught `TypeError` → 500 where the contract says 400) is the exact violation the adjacent new comment at `:31-37` claims to have closed. A nested non-string `text` also silently mis-counts: `countWords({type:"doc",content:[{type:"text",text:7}]})` measured as `1`.
- **Suggested fix:** `if (typeof node.text === "string") return node.text;`. The walker guard is the root-cause fix, since DB reads bypass Zod entirely.
- **Confidence:** High
- **Found by:** Logic & Correctness (`claude-opus-5[1m]`)

### [I3] The `alreadyGone` rename-404 message is destroyed by the very next call, so the 404 arm is silent
- **File:** `packages/client/src/components/OuttakeCard.tsx:146-150`, with `packages/client/src/components/OuttakesPanel.tsx:299-303` and `:404`
- **Bug:** The arm calls `onError(S.alreadyGone)` then `onDeleted(outtake.id)`. `OuttakesPanel.tsx:404` wires `onError={setError}` and `:402` wires `onDeleted={handleDeleted}`, whose body ends `setError(null)` at `:302`. Two writes to the same `useState` in one continuation; the last wins. This holds **regardless of React 18 batching semantics** — batched, the committed value is `null`; unbatched, the message renders for one frame and is then cleared.
- **Impact:** The writer edits an outtake label and blurs; the row was hard-deleted from another tab (or its project trashed); the PATCH 404s; the card disappears with **no message at all**. Commit `db36f8a2`'s stated intent was "drop the card *and say so*, mirroring SnapshotPanel's `isNotFound` arm" — the "say so" half never reaches the DOM. On a hard-delete table with no trash and no 30-day window, "the card vanished and nothing explained why" is the least reassuring possible outcome. `handleDelete`'s 404 arm (`:193-196`) is deliberately silent, so the asymmetry is invisible from the unit test alone — and the only assertion (`OuttakeCard.test.tsx:430`) is against a mocked prop, while `OuttakesPanel.test.tsx` has no 404 case at all.
- **Suggested fix:** Swap the two calls so the message is the last write. Sturdier: stop `handleDeleted`/`handleUpdated` from blanket-clearing `error`. Sturdiest: give the panel a reconciler that takes the message (`onDeleted(id, message?)`) so the two writes are one state transition.
- **Confidence:** High
- **Found by:** Logic & Correctness (`claude-opus-5[1m]`), Error Handling (`claude-opus-5[1m]`)

### [I4] `refreshTrashList`'s drift baseline moved from the operation's project to the *current* project — a regression from the S8 dedup
- **File:** `packages/client/src/hooks/useTrashManager.refresh.ts:40`, consumed at `packages/client/src/hooks/useTrashManager.ts:373`
- **Bug:** `main` captured `const startedForProjectId = project.id` — the **argument**. HEAD line 40 is `makeStaleProjectGuard(projectRef, projectSlugRef)`, and `staleProjectGuard.ts:43-44` captures its baseline from `projectRef.current` / `projectSlugRef.current` at construction. At `useTrashManager.ts:115` (`openTrash`) the swap is baseline-preserving — no await precedes it, so argument and ref agree. At `useTrashManager.ts:373` an `await handleDeleteChapter(...)` precedes it, so the closure's `project` (A) and `projectRef.current` (B) can diverge. Every other site the refactor touched already captured `projectRef.current?.id`, so this is the one site the swap changed meaning at.
- **Impact:** Trash view open; the user confirms a chapter delete; navigates A → B during the awaited delete and B finishes loading. The GET URL uses `project.slug` (**A's**), but the guard's baseline is now B's id/slug, so both checks pass and the result is `{kind:"ok"}` instead of `{kind:"stale"}`. `setTrashedChapters` paints **project A's deleted chapters into project B's trash view**. Clicking Restore then passes `handleRestore`'s own guard (also baselined on B) and its inner `prev.id !== restoreStartedForProjectId` re-check, so `setProject` splices an A chapter into B's `chapters` array and seeds A's chapter id into B's status cache. Main returned `stale` and the caller bailed. `trashOpen` has exactly three writers (`useState` init, `openTrash:119`, `EditorPage:807`) and none is a project-change reset, so the branch is live. The function's doc comment at `:20-22` still asserts the old contract — "`projectRef.current` is captured at entry" is now literally true and semantically wrong for this caller.
- **Suggested fix:** Keep the operation's own project as the baseline — `makeStaleProjectGuard({ current: project }, projectSlugRef)` (the guard only reads `.current`) — or add an early `if (projectRef.current?.id !== project.id) return { kind: "stale" };` before constructing the guard.
- **Confidence:** Medium
- **Found by:** Logic & Correctness (`claude-opus-5[1m]`)

### [I5] `OuttakeCard`'s write-failure arms have no project-drift guard, so project A's banner lands on project B's panel and never clears
- **File:** `packages/client/src/components/OuttakeCard.tsx:147, 161, 169, 202, 205`; consumers `packages/client/src/components/OuttakesPanel.tsx:404-405`; clearing effect `:113-117`
- **Bug:** `OuttakeCard.tsx:67-85` deliberately removed the per-card `useAbortableAsyncOperation` so an ordinary unmount cannot cancel a committed write. That decision is right, but it removes the only staleness arbitration the card had: neither `commitLabel` nor `handleDelete` constructs a drift guard at entry or re-checks `outtake.project_id` after the await, and every post-await reporting arm writes straight into the panel's shared `setError` / `notePossiblyCommitted`. `applyServerRow` **does** carry the guard (`:194`) — but that is the success path only. Commit `b082975d` applied the failure-arm guard to `handleCreate`'s catch (`:271-274`) and not to the card's two writers.
- **Impact:** A slow PATCH from project A settles as a 500 after the writer has switched to B and B's list GET has already resolved. B's panel shows "Couldn't rename that outtake." over B's rows, referring to a row that is not in this project — and **nothing clears it** for the rest of the session: the clearing effect at `:113-117` runs on `[projectId]` and cannot run again, and the load success arm at `:131` clears once and then never re-fires without a mutation or reload. The 500-latency-after-fast-GET ordering is the common case, not the exotic one. The `onPossiblyCommitted` arms are worse: `notePossiblyCommitted` sets `committedNotice`, which `:110-112` documents as deliberately sticky precisely because "an ambiguity warning about A's server state says nothing about B's" — and it fires `requestReload()` against B. `commitLabel`'s `isNotFound` arm also routes to `handleDeleted` → `reconcile()` → `setCommittedNotice(null)`, wiping a legitimate project-B ambiguity notice. Blast radius is bounded — `handleUpdated`'s `map` and `handleDeleted`'s `filter` are id-keyed over UUIDs, so no row aliasing and no data corruption; the damage is confined to the two shared banner channels.
- **Suggested fix:** Funnel `onError` / `onPossiblyCommitted` through a single panel-side wrapper that takes the originating row's `project_id` and drops the call when it no longer matches `projectIdRef.current` (the panel already keeps that ref at `:179-180`) — one guard where all five callers route through, rather than five call-site guards.
- **Confidence:** Medium
- **Found by:** Concurrency & State (`claude-opus-5[1m]`)

### [I6] The unsaved "New outtake" draft is destroyed by an ordinary tab switch, `Ctrl+.`, or a failed create after unmount
- **File:** `packages/client/src/components/OuttakesPanel.tsx:57-58` (state) and `:222-293` (`handleCreate`); rationale comment at `:77-85`
- **Bug:** `ReferencePanel.tsx:134` renders `{activeTab?.panel ?? null}` — only the active tab is mounted — and `EditorMainContent.tsx:384` wraps the whole panel in `{panelOpen && …}`. `OuttakesPanel` holds `showNew`/`draft` in plain `useState` with no `usePersistedState`, no cache, no `beforeunload`. **This branch is what put them in conflict:** it added the second tab (`EditorMainContent.tsx:404-415`) and the arrow-key tab handler that calls `onSelectTab` on keydown (`ReferencePanel.tsx:42-77`), so leaving the tab is now a single keystroke.
- **Impact:** **(a)** Type 400 words into the textarea, press `Ctrl+.` or `→` while a tab button has focus. The panel unmounts, `draft` is discarded, no confirm, no warning. There is no server copy — the POST never fired. **(b, the sharper case)** `handleCreate` deliberately runs the POST with no `AbortSignal` (comment I3) so a mid-flight unmount cannot cancel a committed write. That fixes the *success* leg only — `setError` / `setCommittedNotice` / `setDraft` are all no-ops once the component is gone. The server 500s or hangs, the writer switches tabs inside that window, and the rejection lands on a dead component: empty form, no banner, no row, text unrecoverable. The panel's own comment at `:280-285` states "discarding content the server may never have received is the one failure this panel exists to prevent," and `:77-85` diagnoses this exact unmount path while fixing only the AbortSignal half.
- **Suggested fix:** Lift `showNew`/`draft` into `EditorMainContent`/`EditorPage`, which survive the panel close and the tab switch and already own `capturedOuttake`; or back it with the existing `usePersistedState` seam keyed by project id. Cheaper stopgap: render the panel `hidden` rather than conditionally while `draft.trim()` is non-empty or a create is in flight.
- **Confidence:** Medium
- **Found by:** Error Handling (`claude-opus-5[1m]`), Contract & Integration (`claude-opus-5[1m]`)

## Suggestions

- **[S1]** `packages/client/src/components/OuttakesPanel.tsx:131` — a concurrent list load's success arm clears `error` unconditionally, erasing a write failure the writer has not read. One `error` state serves two channels (list-load and per-card/per-create writes), and a card write failure routes through `applyMappedError` → `onMessage: setError` **without** calling `reconcile()`, so it never bumps `seq` — the in-flight load's token stays fresh and its success arm proceeds to clear. A toolbar capture returning 2xx `BAD_JSON` bumps `externalRefreshKey` → list GET in flight; the writer confirms a card delete inside that window and it 500s → banner; the GET resolves → banner gone, card still on screen, indistinguishable from one whose delete was never clicked. The panel already knows this is a hazard — `:156-159` puts `committedNotice` in separate state for exactly this reason — but applied the reasoning to one channel only. Fix: give the load its own `listError` slot, as `SnapshotPanel.tsx:97` already does. *(Found by Concurrency & State — Medium)*
- **[S2]** `packages/server/src/outtakes/outtakes.repository.ts:21` — the outtake corruption gate is a "walkable?" check, not an "is-a-document?" check. `parseRow` gates on `isTipTapNode(parsed)`, which `tiptap-safety.ts:45` defines as *any* non-null non-array object — deliberately the shared walkability predicate. So `null`/`42`/`[]`/`"text"` are flagged, but `{"foo":1}` and `{"type":"doc","content":{…}}` are returned verbatim with no flag, and list as an empty card with 0 words and **no corruption badge** — the "looks empty, invites the writer to hard-delete the last copy of recoverable JSON" failure the flag was added for. The sibling this file says it mirrors (`snapshots.service.ts:127`) explicitly names this case and gates on `TipTapDocSchema`; the two degrade policies disagree. Reachability is out-of-band only (hand-edited DB or restored backup). Fix: widen the gate to `TipTapDocSchema.safeParse(parsed).success`, keeping the empty-doc + flag degrade in the `catch`. *(Found by Error Handling — Medium)*
- **[S3]** `packages/client/src/errors/scopes.ts:496-503` — `outtake.create` is the one write scope with no 404 arm. `outtakes.routes.ts:53-55` throws `NotFoundError("Project not found.")` when the project was soft-deleted while the editor was open, but the scope declares only `fallback`, `committed`, and `byStatus: { 413 }`. A 404 renders "Failed to save outtake", which reads as transient and invites a retry that 404s identically, forever — with the writer's text sitting in the textarea. The sibling defect was fixed for update and delete by `db36f8a2`; neither `handleCreate` nor `EditorPage`'s capture catch imports `isNotFound`. Compounds I6. Fix: add `404` to the scope's `byStatus` — safe here, since 404 has exactly one producer on this route. *(Found by Error Handling — Medium)*
- **[S4]** `packages/client/src/components/OuttakeCard.tsx:14-29` vs `docs/plans/2026-07-19-scratchpad-outtakes-plan.md:824,826` — `OuttakeCard`'s delivered contract inverts plan Task D1's prop shape with no superseded callout. The plan specifies a presentational card that delegates writes upward; delivered is the opposite split — the card calls `api.outtakes.*` itself and owns both re-entrancy latches, the 404 reconciliation, the error routing and the field-revert policy. Task D2 **does** carry a "Superseded during implementation" callout for its own substitution, so the convention exists and D1 is the exception; D2's `OuttakesPanelProps` block also still shows three fields while the delivered interface has four (`externalRefreshKey`). Matters because design §2 and §6 make re-reading these docs a hard precondition for Phase 4c.2a, the destructive cut — at which point the outtake becomes the sole copy of the text, and an author following Task D1 would assume the *panel* owns abort policy when the delivered card owns it and deliberately carries no AbortSignal. Fix: add a D1 callout naming I3/I4/S5 and the delivered prop set; extend the D2 block. *(Found by Spec Compliance — Medium)*

## Out of Scope

> **Handoff instructions for any agent processing this report:** The findings below are
> pre-existing bugs that this branch did not cause or worsen. Do **not** assume they
> should be fixed on this branch, and do **not** assume they should be skipped.
> Instead, present them to the user **batched by tier**: one ask for all out-of-scope
> Critical findings, one ask for all Important, one for Suggestions. For each tier, the
> user decides which (if any) to address. When you fix an out-of-scope finding, remove
> its entry from `paad/code-reviews/backlog.md` by ID.

### Out-of-Scope Critical
None found.

### Out-of-Scope Important

#### [OOSI1] `validateTipTapDepth` fails open on a null/primitive element inside a nested `content[]` — backlog id: `e66fe50c`
- **File:** `packages/shared/src/tiptap-safety.ts:71` (recursion loop at `:82-84`)
- **Bug:** The entry arm `if (!node || typeof node !== "object") return true;` fires for a `null`/number/string/boolean sitting as an *element of* a `content` array, and the recursion loop does not gate on `isTipTapNode`. `TipTapDocSchema` types top-level elements only (`content: z.array(z.record(z.unknown()))`), so `{"type":"doc","content":[{"type":"paragraph","content":[0]}]}` validates. The branch closed the *container* case (S1) and the *array-child* case (I2) two lines away and left this one open, while the function's new doc comment at `:55-57` asserts a fail-closed contract it does not meet.
- **Impact:** Verified by execution against the pinned `@tiptap/html`: a number or string child makes `renderEditorHtml` throw `RangeError`, `chapterContentToHtml` catches and returns `""`, so HTML/EPUB/markdown/plaintext emit the chapter heading with **no body** behind a single `logger.warn`, while `word_count` reads healthy. DOCX diverges and drops only the one node. Also reachable through `POST /api/snapshots/:id/restore`, and `snapshots.service.ts:145-148` explicitly claims the schema gate catches "malformed nodes (numbers in content, etc.)" — true at the top level only.
- **Out-of-scope ruling:** Lines `:71` and `:82-84` are byte-identical to `main` and outside the touched ranges. The branch's edits to this function strictly *narrowed* the accepted set; no input it newly admits triggers the bug, no walker dropped a guard in reliance on the new contract (all seven independently audited fail-closed), and the new outtakes write path is a second front door to a door already open on `PATCH /api/chapters/:id`. Outtakes render plaintext-only, so the export blast radius is unchanged. Worth surfacing anyway: it is the unclosed sibling of a fix made two lines above, and closing it is a three-line change.
- **Verifier corrections to the specialist's text:** the `null`-child case does **not** throw (it renders `<p></p>`); only number and string children do. The specialist's illustrative payload yields a word count of 0, not 50,000 — the healthy-count scenario requires the poisoned node as a *sibling* paragraph, which the Verifier confirmed (count 10, `chapterContentToHtml` → `""`, one warn).
- **Suggested fix:** Reject a non-object where a node belongs, in the loop rather than at the entry arm: `for (const child of content) { if (!isTipTapNode(child)) return false; if (!validateTipTapDepth(child, depth + 1)) return false; }`. `isTipTapNode` already lives in this file and subsumes the existing `Array.isArray` arm. Also correct the false assurance in `snapshots.service.ts`'s restore comment.
- **Confidence:** High
- **Found by:** Security (`claude-opus-5[1m]`)
- **Backlog status:** new

#### [OOSI2] An irreversible image delete is gated by a scan whose parse failure is swallowed with no signal — backlog id: `89368329`
- **File:** `packages/server/src/images/images.service.ts:174`; twin at `packages/server/src/images/images.references.ts:212`
- **Bug:** `deleteImage` decides between 409-blocked and 204-deleted by parsing every chapter's content and checking `extractImageIds(...).includes(id)`. A chapter whose content does not parse is skipped by an empty `catch` — no log, no counter, no propagation. A chapter whose content parses to a non-object (`"null"`, `"42"`, `"[]"`) never even reaches the catch: `extractImageIds` walks it, finds nothing, returns `[]`. Either way the scan cannot distinguish "no chapter references this image" from "one chapter was never read."
- **Impact:** Chapter C's content is unreadable (hand-edited row, restored backup, partial write) but its text still contains `<img src="/api/images/X">`. `DELETE /api/images/X` skips C silently → `referencingChapters` is empty → `txStore.removeImage(id)` commits → the file is `unlink`ed. **204 instead of 409, and the image row *and* bytes are gone permanently.** Chapter C is itself recoverable — `chapters.repository.parseContent` flags it `content_corrupt` and `chapters.routes.ts` has a designed `CORRUPT_CONTENT` route, so the codebase treats a corrupt chapter as repairable — but after this delete the repair yields a chapter with a permanently broken image. This is the one place in the image lifecycle where a read failure produces an irreversible write.
- **Out-of-scope ruling:** `images.service.ts` touched lines are `5,59,61` — the branch's only edit to that file swaps a local `MAX_SIZE_BYTES` for the shared `MAX_IMAGE_UPLOAD_BYTES` constant, nowhere near `deleteImage`. `images.references.ts:212` is likewise untouched. Neither promotion criterion holds: the branch's `isTipTapNode(child)` guard on `extractImageIds` makes that walker *less* likely to throw, and nothing in the diff routes new inputs into `deleteImage`. "The branch's hardening pass enumerated its sibling parse sites and skipped these two" is a good argument for fixing it, not a promotion criterion — sibling-asymmetry is what the backlog is for.
- **Suggested fix:** Treat an unreadable chapter as a **blocking** reference rather than a non-reference: on parse failure (or `!isTipTapNode(parsed)`) push the chapter into `referencingChapters`, or set an `unreadable` flag forcing the 409 arm, and log `{chapter_id, image_id, project_id}`. Blocking the delete is the conservative failure mode the file already argues for at `images.references.ts:28-35`. Apply the same in `scanImageReferences` so the two agree.
- **Confidence:** Medium
- **Found by:** Error Handling (`claude-opus-5[1m]`)
- **Backlog status:** new

### Out-of-Scope Suggestions

- **[OOSS1]** `packages/client/src/hooks/useChapterCrud.ts:324` — the create-recovery merge arm is the one un-upgraded weak drift guard — backlog id: `ddfa2117`. Every other post-await arm in `handleCreateChapter` (196, 225, 380-387) uses the full-strength `isStaleProject()` built at `:142`; this one kept the id-only compare, which per `staleProjectGuard.ts:14-27` cannot see the pre-load window. On a 2xx `BAD_JSON` or `READ_AFTER_CREATE_FAILURE` create with a slow recovery GET, the whole merge block runs while the user is on B's URL: `setProject(refreshed)` writes A's snapshot, the confirmed-status map is replaced with A's pairs, `setActiveChapter` pins A's new chapter — and any keystroke in that window auto-saves against A's chapter id. **Out-of-scope ruling:** line 324 is not in the file's touched ranges and is unchanged from its pre-branch form; upgrading the siblings does not make this arm fire more often. **Verifier dedup note:** adjacent to existing backlog entry `a65acf76`, which describes the same statement at its pre-move path (`useProjectEditor.ts:800`) — a new ID was minted because two of three match keys fail (the handler body moved files; bug class differs — `a65acf76` is Concurrency about the missing *inside-updater* epoch guard, this is Logic about the *outer* guard's strength). A triager may reasonably collapse them. Fix: `if (!isStaleProject()) { … }`. **Confidence:** Medium. **Found by:** Logic & Correctness (`claude-opus-5[1m]`). **Backlog status:** new.

## Out-of-Scope Additions

> **Handoff instructions for any agent processing this report:** The entries below are code this branch added that the spec did not promise. They may be legitimate "while I'm here" fixes for issues exposed by this work, or scope creep that should live in a separate PR. Do **not** assume they should stay on this branch, and do **not** assume they should be reverted. Present them to the user **as a single batched ask**: "These M additions weren't promised by the spec — keep, split into a separate PR, or revert?" The user decides per item.
>
> Out-of-scope additions are flagged for this PR only — they do not persist to `paad/code-reviews/backlog.md`.

### [OOSA1] Snapshot dedup tie-break rewritten `id DESC` → `rowid DESC`
- **File:** `packages/server/src/snapshots/snapshots.repository.ts:54` and `:80` (commit `9e64c57b`)
- **Addition:** Round 7's `[S8]` was an in-scope finding against `packages/server/src/outtakes/outtakes.repository.ts:81` (the outtakes list tie-breaking on a v4 UUID). The fix commit changed the outtakes site **and** extended the same rewrite sideways into `getLatestContentHash` and `getLatestContentHashAnyKind` — the two chapter-snapshot dedup lookups, a different table and a different feature that outtakes never touch. The commit message states the widening openly. It is a runtime behaviour change: when two snapshots for a chapter share a ms-resolution `created_at`, the row a new snapshot's content hash is compared against moves from arbitrary UUID order to last-inserted, deciding whether the writer's manual marker is accepted or refused as a duplicate. Not already recorded: `snapshots.repository.ts` appears in no cluster OOSA1–OOSA17 (OOSA9 names `snapshots/auto-snapshot.ts`, not the repository's dedup lookups), and `9e64c57b` landed *before* `de286c34` ("record the three out-of-scope entries round 7 found"), so the amendment was written with this widening already on the branch and still did not name it. Same shape as recorded OOSA15. Resolution options: **keep** (record as OOSA18 — the change is correct on the merits and already covered by `snapshots.repository.test.ts` +38) or **revert** the two `snapshots.repository.ts` hunks and land the outtakes-side fix alone.
- **Suggested intent source:** `docs/plans/2026-07-19-scratchpad-outtakes-design.md`, `docs/plans/2026-07-19-scratchpad-outtakes-plan.md`, `docs/roadmap-decisions/2026-07-19-phase-4c-2-scratchpad-outtakes.md` (the recorded one-feature-rule exception and its OOSA1–17 cluster list), plus the 127 branch commit messages.
- **Confidence:** High
- **Found by:** Spec Compliance (`claude-opus-5[1m]`)

## Review Metadata

- **Agents dispatched:** 11 — Logic & Correctness ×2 (client / server+shared), Error Handling & Edge Cases ×2 (client / server+shared), Contract & Integration ×2 (client+wire / server+shared), Concurrency & State ×2 (client / server+shared), Security ×2 (client / server+shared), Spec Compliance ×1 (whole diff vs intent sources), plus 1 Verifier. Partitioned per the large-diff rule.
- **Scope:** 136 changed non-doc files (25 client source, 46 server+shared source, 65 test/e2e), plus callers/callees one level deep and adjacent test files. `.devcontainer/` excluded per CLAUDE.md.
- **Raw findings:** 18 (before verification)
- **Verified findings:** 14 (after verification)
- **Filtered out:** 4 — all duplicate merges across specialists (the DOCX image-id finding was reported independently by three lenses; the `alreadyGone`, draft-loss, and OOSS1-sibling findings by two each). **Zero findings dropped as false positives**, and none fell below the confidence floor.
- **Out-of-scope findings:** 3 (Critical: 0, Important: 2, Suggestion: 1)
- **Out-of-scope additions:** 1
- **Backlog:** 3 new entries added, 0 re-confirmed (see `paad/code-reviews/backlog.md`) — 27 total active
- **Steering files consulted:** `CLAUDE.md` (repo root). No contradiction between the steering file and the code was found; the specialists verified several of its enforced claims directly (typecheck clean, lint clean, no bare `generateHTML()` call site, `editorEntryPointSurface.test.ts` forcing pause intact rather than merely appeased, no hand-rolled `useRef<AbortController>`/`useRef<number>`, no raw `err.message` path to the UI, no inline word-bearing JSX literals).
- **Intent sources consulted:** `docs/plans/2026-07-19-scratchpad-outtakes-design.md`, `docs/plans/2026-07-19-scratchpad-outtakes-plan.md`, `docs/roadmap-decisions/2026-07-19-phase-4c-2-scratchpad-outtakes.md`, `docs/roadmap.md` (Phase 4c.2), and 127 branch commit messages.
- **Verifier warnings:** none
- **Verification note:** The Verifier re-executed the load-bearing claims rather than trusting the specialists — running shapes through the pinned `@tiptap/html` + `starter-kit`, reproducing the `countWords` `TypeError`, confirming SQLite BINARY-collation case sensitivity via `better-sqlite3`, and reading `git show main:` versions of `tiptap-safety.ts`, `wordcount.ts`, `useTrashManager.refresh.ts`, and `docx.renderer.ts` to separate pre-existing defects from branch regressions. Two specialist sub-claims were found wrong and corrected in OOSI1.
